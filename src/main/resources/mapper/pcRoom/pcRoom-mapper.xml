<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pcRoom">
	<select id="pcList" resultType="pcRoom">
		select A.pcNum, A.legend, A.sitNum, substr(A.sitLoc,1,3) as xLoc, substr(A.sitLoc,5,3) as yLoc from pcRoom A left join pcRoomName B on A.pcNum = B.pcNum where pcRoom =  #{pcRoom}
	</select>
</mapper>
